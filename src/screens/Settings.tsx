import { View } from "react-native";
import { Text } from "react-native-paper";

export default function Settings() {
  return (
    <View
      style={{
        padding: 16,
      }}
    >
      <Text>Settings</Text>
    </View>
  );
}
